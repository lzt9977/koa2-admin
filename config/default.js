const config = {
  // 启动端口
  port: 8888,
  // 数据库配置
  database: {
    DATABASE: 'pgt',
    USERNAME: 'root',
    PASSWORD: 'lzt9977',
    PORT: '3306',
    HOST: 'localhost'
  }
}

module.exports = config